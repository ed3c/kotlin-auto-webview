# Contributing

Use focused branches and small pull requests. Describe the invariant being protected, the platform impact, and verification evidence. New dependencies require license, maintenance, platform, binary-size, and privacy review.

Run the verification contract in `AGENTS.md`. Security-sensitive changes should include negative tests and a threat-model update.
