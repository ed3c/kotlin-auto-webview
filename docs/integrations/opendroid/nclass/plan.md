# OpenDroid integration reduction plan

Class: N  
Authority: none  
Mandatory route: no  
Snapshot: 2026-09-11

This is a disposable planning projection. GitHub Issues, exact commits/trees, executable checks, and provider readback remain authoritative.

## Bottleneck order

1. **Document-route atom.** Replace the root's long mandatory read list with the three-node route, stable requirement owner, Issue atom template, and planted fourth-hop gate. This changes no Android capability.
2. **Current-main integration atom.** Select the useful bytes from the old Draft stack `#76 → #137 → #151 → #154 → #157`, replay them onto current `main`, and run all baseline plus applicable Android evidence at the new exact head. Do not merge the stale chain one PR at a time.
3. **Physical handoff atom (`#186`).** Only after a current-main candidate exists, bind repository-owned argv/cwd/timeouts and exercise the admitted physical-device lane. Accessibility enablement and device trust remain Human gates; Shizuku stays `NOT_IMPLEMENTED` until typed implementation and an oracle exist.
4. **Store/release admission.** Run only after implementation and physical denominators are explicit. This is external authority, not a documentation or CI transition.

## Issue hygiene

Split an Issue when it mixes implementation, evidence, documentation, and external admission. Each executable atom carries one requirement, one write lease, one positive control, one planted-negative control, and one evidence ceiling. Superseded Draft PRs remain historical evidence carriers; they are not silently relabeled as current-main proof.

