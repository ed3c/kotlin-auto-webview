# Creator Capability Plane — Agent contract

This directory owns the creator-content route from admitted source observations to v7.2 cards, Procedural IR, independently qualified Skill variants, Community Skill Editions and user-outcome foldback.

It does not own generic WebView rendering, repository-wide policy, root indexes outside issue #98, legal/platform/store approval, provider accounts, physical-device access or merge authority.

## Read order

1. Root `AGENTS.md` and `README.md`.
2. `docs/security/CONTENT_PLATFORM_MEDIA_RISK_REGISTER.md`.
3. `COMMUNITY_SKILL_EDITION_ARCHITECTURE.md`.
4. `PROCEDURAL_SKILL_COMPILER_SYSTEM_PROMPT.md`.
5. This directory `README.md`.
6. `CURRENT_STATE.md`, `CREATOR_CAPABILITY_DAG.md`, `MOLECULAR_STACK_INDEX.md`.
7. Exact issue/task packet, branch/PR/head/tree and nearest code README/AGENTS.

## Current truth

```text
prompt                              materialized on main@290a82f...
risk design                         #80 / Draft PR #81
Community Edition design            #82 / Draft PR #83
runtime/contracts/adapters/editor    NOT_IMPLEMENTED (#84–#97, #102–#110)
root/creator docs owner              #98
local Git Town/checks                NOT_EXERCISED / BLOCKED_POLICY
legal/platform/store/device          EXTERNAL_AUTHORITY_REQUIRED / NOT_EXERCISED
```

## Authority laws

```text
source adapter != rights authority
model != source truth or execution authority
card != causal law
compiler != qualifier
community preference != evidence
player/embed/Media Integrity != content rights or Premium
technical PASS != market outcome
```

Every operation separately decides render, navigate, observe, retain locator/excerpt/full source, capture frame, external-model egress, derive card/Skill, private share and publication.

## Source-specific ceilings

- YouTube: official foreground/reference lane; no download, hidden montage, ad/control modification, background/system PiP or Premium/session claim.
- PDF/EPUB/local: possession/viewing is not ownership or publication permission; DRM/protected content fails closed.
- Notion/Drive/Docs: organization/admin/DLP and model-destination authority remain explicit; OAuth/session tokens never enter artifacts.
- X: observation-only website lane; actions require separately admitted official API.
- Generic Web: origin/CSP/cross-origin/robots/anti-bot limits are surfaced, never bypassed.

## Compiler and qualification

The v7.2 indexer automatically creates stable atomic cards and typed evidence links. Human curation may select/merge/reorder but cannot rewrite source identity, rights or evidence class silently.

Compiler output is candidate Procedural IR. Qualifier independently enforces:

```text
G1 executable
G2 discriminative
G3 stateful
G4 falsifiable
G5 observable
G6 transferable
G7 evidence-bound
G8 compression-value
```

Any FAIL returns `NOT_QUALIFIED`.

## Community laws

SkillPatch and community content are untrusted data. Preserve contributor identity, contribution grant, selected cards, procedure delta, evidence, conflicts, moderation and qualification. Votes do not change evidence. Public release requires executable UGC controls and revocation/takedown propagation. Licensed rendering begins blocked without exact rights.

## Worker rules

- Work only within the exact issue, branch and path lease.
- Use one chosen Git parent for consumed bytes; record cross-leaf completion dependencies separately.
- Do not edit shared indexes from leaves. #98 is current shared writer.
- Design evals and planted negative controls before implementation.
- Run Shadow checkpoints at architecture, authority/rights, public contract, external integration, first slice/green and publication boundaries.
- Preserve all non-PASS and external-authority lanes in handoff.
- Never invent local commands, receipts, branch existence or completion.

## Required output packet

```text
exact issue / branch / base / head / tree
changed paths and state transitions
positive evals and planted controls
exact results and evidence ceiling
remaining FAIL / ABSENT / NOT_IMPLEMENTED / NOT_EXERCISED
external-authority operations
cleanup / rollback subject
next safe atom
```
